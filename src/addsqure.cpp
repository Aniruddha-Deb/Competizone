#include <bits/stdc++.h>
using namespace std;

int main() {
	int w, h, n, m;
	cin >> w >> h >> n >> m;
	int t;
	vector<int> N(n);
	vector<int> M(m);
	for (int i=0; i<n; i++) {
		cin >> t;
		N.push_back(t);
	}
	for (int i=0; i<m; i++) {
		cin >> t;
		M.push_back(t);
	}


	return 0;
}

#include <bits/stdc++.h>
using namespace std;

typedef long long ll

void init() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
}

int A[105][105];
int E[10005];

int main() {
	init();
	int t;
	cin >> t;
	while (t-- > 0) {
		int n, m;
		cin >> n >> m;
		for (int i=0; i<n; i++) {
			for (int j=0; j<m; j++) {
				cin >> A[i][j];
				E[c] = A[i][j];
			}
		}
		
	}
	return 0;
}
